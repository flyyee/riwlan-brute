# riwlan-brute
Brute forces login credentials of RI-WLAN. Contains outline of how network works. Brute-force method is still slow, however. 
Never tested on actual network, as it should never be.  
Includes mock node server program can be tested on.
